@model IEnumerable<SimsWeb.ViewModels.FacultyListItemViewModel>

@{
    ViewData["Title"] = "Manage Faculty";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h4 class="mb-0">Manage Faculty</h4>
    <div>
        <a asp-controller="Faculty" asp-action="Deleted"
           class="btn btn-sm btn-outline-secondary me-2">
            Recycle Bin
        </a>
        <a asp-controller="Faculty" asp-action="Create"
           class="btn btn-sm sims-btn-orange">
            + Add Faculty
        </a>
    </div>
</div>

<div class="card shadow-sm border-0">
    <div class="card-body p-3">
        <div class="table-responsive">
            <table class="table table-sm align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th>#</th>
                        <th>Code</th>
                        <th>Full Name</th>
                        <th>Email</th>
                        <th>Department</th>
                        <th>Title</th>
                        <th>Created</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @if (!Model.Any())
                    {
                        <tr>
                            <td colspan="8" class="text-center text-muted small py-3">
                                No faculty found.
                            </td>
                        </tr>
                    }
                    else
                    {
                        var i = 1;
                        foreach (var f in Model)
                        {
                            <tr>
                                <td>@i</td>
                                <td>@f.FacultyCode</td>
                                <td>@f.FullName</td>
                                <td>@f.Email</td>
                                <td>@f.Department</td>
                                <td>@f.Title</td>
                                <td>@f.CreatedAt.ToLocalTime().ToString("yyyy-MM-dd HH:mm")</td>
                                <td class="text-end">
                                    <a asp-controller="Faculty" asp-action="Edit"
                                       asp-route-id="@f.Id"
                                       class="btn btn-sm btn-outline-primary me-1">
                                        Edit
                                    </a>

                                    <form asp-controller="Faculty" asp-action="Delete"
                                          asp-route-id="@f.Id"
                                          method="post" style="display:inline;"
                                          onsubmit="return confirm('Soft delete this faculty?');">
                                        <button class="btn btn-sm btn-outline-danger">
                                            Delete
                                        </button>
                                    </form>
                                </td>
                            </tr>
                            i++;
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
