@model IEnumerable<SimsWeb.ViewModels.StudentListItemViewModel>

@{
    ViewData["Title"] = "Deleted Students";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h4 class="mb-0">Recycle Bin - Students</h4>
    <a asp-controller="Students" asp-action="Index"
       class="btn btn-sm btn-outline-secondary">
        Back to Students
    </a>
</div>

<div class="card shadow-sm border-0">
    <div class="card-body p-3">
        <div class="table-responsive">
            <table class="table table-sm align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th>#</th>
                        <th>Code</th>
                        <th>Full Name</th>
                        <th>Email</th>
                        <th>Class</th>
                        <th>Created</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @if (!Model.Any())
                    {
                        <tr>
                            <td colspan="7" class="text-center text-muted small py-3">
                                No deleted students.
                            </td>
                        </tr>
                    }
                    else
                    {
                        var i = 1;
                        foreach (var s in Model)
                        {
                            <tr>
                                <td>@i</td>
                                <td>@s.StudentCode</td>
                                <td>@s.FullName</td>
                                <td>@s.Email</td>
                                <td>@s.CreatedAt.ToLocalTime().ToString("yyyy-MM-dd HH:mm")</td>
                                <td class="text-end">
                                    <form asp-controller="Students" asp-action="Restore"
                                          asp-route-id="@s.Id"
                                          method="post" style="display:inline;">
                                        <button class="btn btn-sm btn-outline-success me-1">
                                            Restore
                                        </button>
                                    </form>

                                    <form asp-controller="Students" asp-action="HardDelete"
                                          asp-route-id="@s.Id"
                                          method="post" style="display:inline;"
                                          onsubmit="return confirm('Permanently delete this student?');">
                                        <button class="btn btn-sm btn-outline-danger">
                                            Delete Permanently
                                        </button>
                                    </form>
                                </td>
                            </tr>
                            i++;
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
